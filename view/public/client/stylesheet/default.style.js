import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  // ... các kiểu dáng khác
  searchContainer: {
    paddingTop: 20, // Thêm padding top vào vùng chứa
    // ... các kiểu dáng hiện có
  },
  // ... các kiểu dáng khác
});

export default styles; 